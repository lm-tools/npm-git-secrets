{
  "name": "npm-git-secrets",
  "version": "1.0.2",
  "description": "installs and configures git secrets",
  "main": "index.js",
  "scripts": {
    "postinstall": "PATH=$PATH:node_modules/.bin && git secrets --install -f && git secrets --register-aws || (echo -e 'ERROR: preinstall failed! git secrets may not be configured correctly.' && exit 1)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lm-tools/npm-git-secrets.git"
  },
  "license": "ISC",
  "homepage": "https://github.com/lm-tools/npm-git-secrets#readme",
  "bin": {
    "git-secrets": "./git-secrets"
  }
}
